<!DOCTYPE html>
<html lang="en">
<head>
    <title>Search Results</title>
    <script src="https://cdn.tailwindcss.com"></script> <!-- Tailwind CSS -->
</head>
<body class="bg-gray-100 p-5">

    <div class="max-w-3xl mx-auto bg-white p-6 rounded-lg shadow-lg">
        <h1 class="text-3xl font-bold text-center text-blue-600 mb-4">Search Results</h1>

        <% if (pokemon) { %>
            <div class="text-center">
                <img src="<%= pokemon.sprites.front_default %>" alt="<%= pokemon.name %>"
                     class="mx-auto w-32 h-32">
                <p class="mt-2 text-xl font-bold text-gray-700"><%= pokemon.name.toUpperCase() %></p>
            </div>
        <% } else { %>
            <p class="text-center text-red-500">No Pokémon found.</p>
        <% } %>

        <div class="mt-4 text-center">
            <a href="/" class="bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">
                Back to Home
            </a>
        </div>
    </div>

</body>
</html>
