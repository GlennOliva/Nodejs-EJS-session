<!DOCTYPE html>
<html lang="en">
<head>
    <title><%= pokemon.name.toUpperCase() %> Details</title>
    <script src="https://cdn.tailwindcss.com"></script> <!-- Tailwind CSS -->
</head>
<body class="bg-gray-100 p-5">

    <div class="max-w-xl mx-auto bg-white p-6 rounded-lg shadow-lg">
        <h1 class="text-3xl font-bold text-center text-blue-600 mb-4"><%= pokemon.name.toUpperCase() %></h1>

        <div class="text-center">
            <img src="<%= pokemon.sprites.front_default %>" alt="<%= pokemon.name %>"
                 class="mx-auto w-32 h-32">
        </div>

        <div class="mt-4">
            <p class="text-lg"><strong>Type(s):</strong> 
                <% pokemon.types.forEach(type => { %>
                    <span class="bg-gray-300 px-2 py-1 rounded-lg"><%= type.type.name %></span>
                <% }); %>
            </p>

            <p class="text-lg"><strong>Height:</strong> <%= pokemon.height / 10 %> m</p>
            <p class="text-lg"><strong>Weight:</strong> <%= pokemon.weight / 10 %> kg</p>
        </div>

        <div class="mt-4 text-center">
            <a href="/pokemon/api" class="bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">
                Back to Home
            </a>
        </div>
    </div>

</body>
</html>
